[package]
name = "fuchsia-config"
edition.workspace = true
version.workspace = true
description = "Workflow configuration types for Fuchsia - serializable workflow definitions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
